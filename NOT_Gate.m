function NOT=NOT_Gate(a)
    if (a>1)
        NOT=("invalid input for a");
    elseif (a<0)
        NOT=("invalid input for a");
        
    elseif (a==1)
            NOT=0;
    elseif (a==0)
        NOT=1;
    end    
    
